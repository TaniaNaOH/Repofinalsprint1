var mensaje = prompt ("Escriba el mensaje a cifrar", " " );{      

      function cesar ('str, key');{

            var cipher=(x-65+n)%26+65;

            var descipher=(x-n)%26;

            str = str.toLowerCase();

            for(var i = 0; i < str.length; i++){

                if(cipher.cesar(str.charAt(i){

                    descipher= str.charAt(i);

                }

                else{

                    descipher+= String.CharCodeAt((x-n)%26 );
                }

            }

            return descipher;

        }

}  

        var descipher = cesar('Tania',33);

       console.log;
